<!DOCTYPE html>
<html>

  <head>
  <%= render 'layouts/header' %>
  </head>

  <body>
    <div class="container">
      <%= render 'layouts/navbar' %>

      <%= render 'layouts/messages' %>

      <%= yield %>

      <%= render 'layouts/footer' %>
    </div>
  </body>

</html>
